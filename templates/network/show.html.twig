{% extends 'base.html.twig' %}

{% block title %}Network{% endblock %}

{% block body %}
    <h1>Network</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ network.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ network.name }}</td>
            </tr>
            <tr>
                <th>IsGuest</th>
                <td>{{ network.isGuest ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('network_index') }}">back to list</a>

    <a href="{{ path('network_edit', {'id': network.id}) }}">edit</a>

    {{ include('network/_delete_form.html.twig') }}
{% endblock %}
